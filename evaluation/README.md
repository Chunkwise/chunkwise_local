# To test the evaluation function locally or call the manual_evaluate endpoint:
- generate an OPENAI API key (for calling the embedding model)
- create a .env file with:
    OPENAI_API_KEY=[your_api_key]