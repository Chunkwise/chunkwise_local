{
  "name": "Python 3.13 + SQLite 3.35.0 + Poetry",
  "build": {
    "dockerfile": "Dockerfile",
    "args": {
      "PYTHON_VERSION": "3.13.0"
    }
  },
  "remoteUser": "vscode",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  "postCreateCommand": "python -c \"import sqlite3, sys; print('Python:', sys.version); print('SQLite:', sqlite3.sqlite_version)\" && sqlite3 --version && poetry --version"
}