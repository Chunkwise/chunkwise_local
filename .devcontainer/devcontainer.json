{
  "build": {
    "dockerfile": "Dockerfile",
    "args": {
      "PYTHON_VERSION": "3.13.0"
    }
  },
  "postCreateCommand": "python -c \"import sqlite3, sys; print('Python:', sys.version); print('SQLite:', sqlite3.sqlite_version)\""
}
